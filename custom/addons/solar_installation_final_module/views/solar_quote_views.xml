<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Action for Solar Quotes -->
        <record id="action_solar_quote" model="ir.actions.act_window">
            <field name="name">Solar Quotations</field>
            <field name="res_model">solar.quote</field>
            <field name="view_mode">form</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create your first solar quotation
                </p>
            </field>
        </record>

        <!-- Form View -->
        <record id="view_solar_quote_form" model="ir.ui.view">
            <field name="name">solar.quote.form</field>
            <field name="model">solar.quote</field>
            <field name="arch" type="xml">
                <form string="Solar Quote">
                    <header>
                        <button string="Send" type="object" name="action_send" states="draft" class="oe_highlight"/>
                        <button string="Accept" type="object" name="action_accept" states="sent" class="oe_highlight"/>
                        <button string="Refuse" type="object" name="action_refuse" states="draft,sent"/>
                        <button string="Reset to Draft" type="object" name="action_reset_to_draft" states="refused,expired"/>
                        <button string="Mark as Expired" type="object" name="action_mark_expired" states="sent"/>
                        <field name="state" widget="statusbar" statusbar_visible="draft,sent,accepted,refused,expired"/>
                    </header>
                    <sheet>
                        <div class="oe_title">
                            <h1>
                                <field name="quote_number" readonly="1"/>
                            </h1>
                        </div>
                        <group>
                            <group>
                                <field name="project_id"/>
                                <field name="partner_id"/>
                                <field name="currency_id" invisible="1"/>
                            </group>
                            <group>
                                <field name="quote_date"/>
                                <field name="validity_days"/>
                                <field name="expiration_date"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="Quote Lines">
                                <field name="quote_line_ids">
                                    <tree editable="bottom">
                                        <field name="product_id"/>
                                        <field name="description"/>
                                        <field name="quantity"/>
                                        <field name="unit_price"/>
                                        <field name="discount_pct"/>
                                        <field name="price_subtotal" sum="Total"/>
                                    </tree>
                                </field>
                                <group class="oe_subtotal_footer oe_right">
                                    <field name="untaxed_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                                    <field name="tax_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                                    <div class="oe_subtotal_footer_separator oe_inline">
                                        <label for="total_amount"/>
                                    </div>
                                    <field name="total_amount" widget="monetary" options="{'currency_field': 'currency_id'}" class="oe_subtotal_footer_separator"/>
                                </group>
                                <div class="oe_clear"/>
                            </page>
                            <page string="Notes">
                                <group>
                                    <field name="notes" placeholder="Notes visible to the customer..."/>
                                    <field name="internal_notes" placeholder="Internal notes (not visible to customer)..."/>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids"/>
                        <field name="activity_ids"/>
                        <field name="message_ids"/>
                    </div>
                </form>
            </field>
        </record>
    </data>
</odoo>