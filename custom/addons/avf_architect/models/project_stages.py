# -*- coding: utf-8 -*-

from odoo import models, fields, api, _

# Custom project stages model removed - using project.task.type instead
# This avoids foreign key conflicts and follows Odoo best practices