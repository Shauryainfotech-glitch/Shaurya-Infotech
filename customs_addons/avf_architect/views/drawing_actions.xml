<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_drawing_history" model="ir.actions.act_window">
        <field name="name">Drawing History</field>
        <field name="res_model">architect.drawing</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('id', 'child_of', active_id)]</field>
        <field name="context">{}</field>
    </record>
</odoo>